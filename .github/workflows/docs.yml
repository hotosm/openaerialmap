name: 📖 Publish Docs

on:
  push:
    paths:
      - docs/**
      - mkdocs.yml
      - backend/stactools-hotosm/stac-extension/json-schema/**/schema.json
    branches: [main]
  # Allow manual trigger (workflow_dispatch)
  workflow_dispatch:

jobs:
  publish_docs:
    uses: hotosm/gh-workflows/.github/workflows/mkdocs_build.yml@4.1.0
