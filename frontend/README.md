# OpenAerialMap

- A frontend that uses [stac-map](github.com/developmentseed/stac-map) underneath.
- Feature parity with the old custom frontend will come in time.

## Development

```bash
pnpm install
pnpm run dev
```
