[project]
name = "tilepack-api-worker"
version = "0.1.0"
description = "Generate mbtiles and pmtiles files from STAC-based COG imagery."
readme = "../README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3==1.42.85",
    "httpx==0.28.1",
    "rasterio==1.5.0",
    "rio-tiler==9.0.5",
]
