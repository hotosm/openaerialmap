[project]
name = "oam-pipeline-convert"
version = "0.1.0"
requires-python = ">=3.12"
# Floors express what this step needs; pipeline/uv.lock holds the exact pins.
dependencies = ["rasterio>=1.5", "rio-cogeo>=7"]
