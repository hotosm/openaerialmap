[project]
name = "oam-pipeline-validate"
version = "0.1.0"
requires-python = ">=3.12"
# Floors express what this step needs; pipeline/uv.lock holds the exact pins.
dependencies = ["numpy>=2", "rasterio>=1.5"]
